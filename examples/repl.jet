(define repl
  (lambda ()
    (print ">> ")
    (let ((read-form (read)))
      (if (not (eof-object? read-form))
        (begin
          (print "==> ")
          (println (eval read-form))
          (repl))
        (println "bye!")))))

(repl)